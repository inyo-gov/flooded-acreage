---
title: "Blackrock Waterfowl Management Area"
subtitle: "Flooded acreage monitoring dashboard"
format:
  html:
    code-tools: false
    toc: false
---

```{=html}
<div class="dashboard-hero" style="margin:0.5rem 0 2rem;padding:2.25rem 2rem;border-radius:14px;background:linear-gradient(135deg,#1a5f7a 0%,#2e86ab 55%,#3d9cad 100%);color:#fff;box-shadow:0 4px 24px rgba(26,95,122,0.08);position:relative;overflow:hidden;">
  <div class="hero-content" style="position:relative;z-index:1;">
    <h1 style="color:#fff;font-size:clamp(1.75rem,4vw,2.35rem);margin:0 0 0.5rem;line-height:1.15;">Blackrock Waterfowl Management Area</h1>
    <p class="hero-lead" style="margin:0;font-size:1.05rem;color:rgba(255,255,255,0.92);max-width:42rem;">Monthly flooded acreage estimates from Sentinel-2 surface reflectance — Owens Valley, Inyo County, California.</p>
    <div class="hero-meta" style="display:flex;flex-wrap:wrap;gap:0.5rem 1rem;margin-top:1.25rem;font-size:0.875rem;">
      <span style="display:inline-flex;padding:0.35rem 0.75rem;border-radius:999px;background:rgba(255,255,255,0.14);">Sentinel-2 optical · NIR threshold</span>
      <span style="display:inline-flex;padding:0.35rem 0.75rem;border-radius:999px;background:rgba(255,255,255,0.14);">15-day cloud-masked composite</span>
    </div>
  </div>
</div>
```

```{r read_csvs, echo=FALSE, message=FALSE, warning=FALSE}
library(dplyr)
library(readr)
library(stringr)
library(lubridate)

csv_dir <- "flood_reports/csv_output"

csv_files <- list.files(
  path       = csv_dir,
  pattern    = "^flood_report_data_[0-9]{4}-[0-9]{2}-[0-9]{2}_.*\\.csv$",
  full.names = TRUE
)

monthly_raw <- lapply(csv_files, function(path) {
  fname     <- basename(path)
  date_part <- str_match(
    fname,
    "^flood_report_data_([0-9]{4}-[0-9]{2}-[0-9]{2})_.*\\.csv$"
  )[, 2]
  report_dt <- as.Date(date_part, format = "%Y-%m-%d")

  read_csv(path, show_col_types = FALSE) %>%
    mutate(report_date = report_dt)
}) %>%
  bind_rows() %>%
  select(Flood_Unit, acres_flooded, report_date)
```

```{r dashboard_stats, echo=FALSE, message=FALSE, warning=FALSE}
#| classes: stat-chunk
library(dplyr)
library(lubridate)
library(htmltools)

fmt_acres <- function(x) format(round(x, 1), nsmall = 1, trim = TRUE)

stat_card_style <- "background:#f7fafb;border:1px solid #d8e3ea;border-radius:14px;padding:1.15rem 1.25rem;box-shadow:0 4px 24px rgba(26,95,122,0.08);"
stat_highlight_style <- paste0(
  stat_card_style,
  "border-color:#2e86ab;background:linear-gradient(180deg,#ffffff 0%,#e8f4f8 100%);"
)
stat_grid_style <- "display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1rem;margin-bottom:2rem;"
stat_label_style <- "font-size:0.78rem;font-weight:600;letter-spacing:0.04em;text-transform:uppercase;color:#5c6b73;margin-bottom:0.35rem;"
stat_value_style <- "font-size:1.85rem;font-weight:700;line-height:1.1;color:#1a5f7a;"
stat_detail_style <- "margin-top:0.45rem;font-size:0.875rem;color:#5c6b73;line-height:1.45;"

totals <- monthly_raw %>%
  filter(Flood_Unit == "Total") %>%
  arrange(desc(report_date))

latest <- totals %>% slice(1)
prev   <- totals %>% slice(2)

latest_month <- floor_date(latest$report_date, "month")
season_peak <- monthly_raw %>%
  filter(Flood_Unit == "Total", report_date >= as.Date("2024-09-01")) %>%
  mutate(month_date = floor_date(report_date, "month")) %>%
  filter(month(report_date) %in% c(11, 12, 1, 2, 3)) %>%
  group_by(month_date) %>%
  slice_max(report_date, n = 1, with_ties = FALSE) %>%
  ungroup() %>%
  slice_max(acres_flooded, n = 1, with_ties = FALSE)

delta_detail <- if (nrow(prev) == 1) {
  delta <- latest$acres_flooded - prev$acres_flooded
  cls <- if (delta >= 0) "stat-delta-up" else "stat-delta-down"
  sign <- if (delta >= 0) "+" else ""
  tags$span(
    class = cls,
    paste0(sign, fmt_acres(delta), " ac vs prior month")
  )
} else {
  tags$span("First report in series")
}

season_value <- if (nrow(season_peak) == 1) {
  paste0(
    fmt_acres(season_peak$acres_flooded),
    " ac on ",
    format(season_peak$report_date, "%b %Y")
  )
} else {
  "—"
}

mom_value <- if (nrow(prev) == 1) {
  paste0(fmt_acres(abs(latest$acres_flooded - prev$acres_flooded)), " ac change")
} else {
  "—"
}

tags$div(
  class = "stat-grid",
  style = stat_grid_style,
  tags$div(
    class = "stat-card stat-highlight",
    style = stat_highlight_style,
    tags$div(class = "stat-label", style = stat_label_style, "Latest total flooded"),
    tags$div(class = "stat-value", style = stat_value_style, paste0(fmt_acres(latest$acres_flooded), " ac")),
    tags$div(
      class = "stat-detail",
      style = stat_detail_style,
      paste0(
        format(latest_month, "%B %Y"),
        " · scene ",
        format(latest$report_date, "%b %d, %Y")
      )
    )
  ),
  tags$div(
    class = "stat-card",
    style = stat_card_style,
    tags$div(class = "stat-label", style = stat_label_style, "Month-over-month"),
    tags$div(class = "stat-value", style = stat_value_style, mom_value),
    tags$div(class = "stat-detail", style = stat_detail_style, delta_detail)
  ),
  tags$div(
    class = "stat-card",
    style = stat_card_style,
    tags$div(class = "stat-label", style = stat_label_style, "Season peak (Nov–Mar)"),
    tags$div(class = "stat-value", style = stat_value_style, season_value),
    tags$div(class = "stat-detail", style = stat_detail_style, "500 ac management target")
  ),
  tags$div(
    class = "stat-card",
    style = stat_card_style,
    tags$div(class = "stat-label", style = stat_label_style, "Reporting units"),
    tags$div(class = "stat-value", style = stat_value_style, "7"),
    tags$div(
      class = "stat-detail",
      style = stat_detail_style,
      "Thibaut, Drew, Waggoner, West Winterton, East Winterton, South Winterton, Thibaut Ponds"
    )
  )
)
```

::: {.dashboard-section}
::: {.dashboard-section-header}
## Monthly flooded acreage by unit

Stacked totals by flood unit. Dashed line marks the **500 ac** seasonal target (Nov 1 → Mar 1).
:::

::: {.dashboard-card .chart-card}
```{r monthly_flooded_acres, echo=FALSE, message=FALSE, warning=FALSE, results='hide', fig.width=10, fig.height=5.5, fig.align='center'}
library(dplyr)
library(lubridate)
library(ggplot2)

unit_colors <- c(
  "Thibaut"         = "#1a5f7a",
  "Drew"            = "#2e86ab",
  "Waggoner"        = "#57a773",
  "West Winterton"  = "#7eb8a4",
  "East Winterton"  = "#a8c686",
  "South Winterton" = "#c4a35a",
  "Thibaut Ponds"   = "#8b6fad"
)

monthly_fixed <- monthly_raw %>%
  mutate(
    Flood_Unit = recode(Flood_Unit,
      "Waggonner" = "Waggoner",
      .default    = Flood_Unit
    ),
    month_date = floor_date(report_date, unit = "month")
  )

monthly_avg <- monthly_fixed %>%
  filter(Flood_Unit != "Total") %>%
  filter(month_date >= as.Date("2024-09-01")) %>%
  group_by(Flood_Unit, month_date) %>%
  slice_max(report_date, n = 1, with_ties = FALSE) %>%
  ungroup() %>%
  select(Flood_Unit, month_date, acres_flooded) %>%
  rename(avg_acres = acres_flooded)

target_start <- as.Date("2024-11-01")
target_end   <- max(monthly_avg$month_date, na.rm = TRUE) + 15

monthly_avg %>%
  ggplot(aes(x = month_date, y = avg_acres, fill = Flood_Unit)) +
  geom_col(width = 22, color = "white", linewidth = 0.15) +
  geom_hline(
    yintercept = 500,
    linetype   = "dashed",
    color      = "#b8860b",
    linewidth  = 0.7
  ) +
  annotate(
    "text",
    x     = target_start + 45,
    y     = 535,
    label = "500 ac target (Nov → Mar)",
    size  = 3.2,
    color = "#8a6914",
    hjust = 0
  ) +
  scale_fill_manual(values = unit_colors, name = "Flood unit") +
  scale_x_date(
    limits      = c(min(monthly_avg$month_date) - 15, max(monthly_avg$month_date) + 15),
    date_breaks = "1 month",
    date_labels = "%b '%y",
    expand    = c(0.02, 0)
  ) +
  scale_y_continuous(expand = expansion(mult = c(0, 0.04)), labels = scales::comma) +
  labs(
    title = NULL,
    x     = NULL,
    y     = "Acres flooded"
  ) +
  theme_minimal(base_size = 13, base_family = "sans") +
  theme(
    panel.grid.major.x = element_blank(),
    panel.grid.minor   = element_blank(),
    panel.grid.major.y = element_line(color = "#e8eef2", linewidth = 0.4),
    axis.text.x        = element_text(angle = 45, hjust = 1, color = "#5c6b73"),
    axis.text.y        = element_text(color = "#5c6b73"),
    axis.title.y       = element_text(color = "#1f2933", face = "bold"),
    legend.position    = "bottom",
    legend.title       = element_text(face = "bold", size = 10),
    legend.text        = element_text(size = 9),
    legend.key.size    = unit(0.45, "cm"),
    plot.margin        = margin(8, 12, 8, 8)
  )
```
:::
:::

::: {.dashboard-section}
::: {.dashboard-section-header}
## Monthly report archive

Each cell links to the full acreage report for that month (day of scene shown).
:::

::: {.dashboard-card .table-card}
```{r monthly_raw, echo=FALSE, message=FALSE, warning=FALSE}
library(dplyr)
library(lubridate)
library(tidyr)
library(knitr)
library(stringr)

report_scan_dir <- "flood_reports/reports"
report_link_dir <- "flood_reports/reports"

html_files <- list.files(
  path       = report_scan_dir,
  pattern    = "^bwma_flood_report_[0-9]{4}-[0-9]{2}-[0-9]{2}_.*\\.html$",
  full.names = FALSE
)

df_links <- tibble(file = html_files) %>%
  mutate(
    date       = as.Date(str_extract(file, "[0-9]{4}-[0-9]{2}-[0-9]{2}")),
    year       = year(date),
    month_num  = month(date),
    month_name = month(date, label = TRUE, abbr = TRUE),
    fy_num     = if_else(month_num >= 7, year, year - 1L),
    fy_label   = paste0(
      fy_num, "-",
      str_sub(as.character(fy_num + 1L), 3, 4)
    ),
    day_only   = format(date, "%d"),
    full_link  = paste0(
      "<a href='", report_link_dir, "/", file,
      "' target='_blank' title='", format(date, "%B %d, %Y"), "'>", day_only, "</a>"
    )
  )

df_one_per_month <- df_links %>%
  group_by(fy_label, month_name) %>%
  slice_min(date, n = 1, with_ties = FALSE) %>%
  ungroup()

all_months_ord <- ordered(month.abb, levels = month.abb)

wide_tbl <- df_one_per_month %>%
  select(fy_label, month_name, full_link) %>%
  complete(
    fy_label,
    month_name = all_months_ord,
    fill       = list(full_link = "")
  ) %>%
  mutate(
    month_for_fy = factor(
      as.character(month_name),
      levels = c(
        "Jul", "Aug", "Sep", "Oct", "Nov", "Dec",
        "Jan", "Feb", "Mar", "Apr", "May", "Jun"
      )
    )
  ) %>%
  select(fy_label, month_for_fy, full_link) %>%
  pivot_wider(
    names_from   = month_for_fy,
    values_from  = full_link,
    names_expand = TRUE
  ) %>%
  arrange(fy_label) %>%
  select(fy_label, any_of(c("Jul", "Aug", "Sep", "Oct", "Nov", "Dec", "Jan", "Feb", "Mar", "Apr", "May", "Jun"))) %>%
  rename(`Year` = fy_label)

knitr::kable(
  wide_tbl,
  format     = "html",
  escape     = FALSE,
  caption    = "Monthly Reports",
  table.attr = "class='table dashboard-reports-table'"
)
```

<p class="table-legend">Click a day number to open the full HTML report (map, unit breakdown, and imagery metadata).</p>
:::
:::

```{=html}
<!-- Latest monthly run: July 2026 (threshold 0.14). June: flood_reports/reports/bwma_flood_report_2026-06-01_0.14.html -->
<div class="dashboard-section">
  <div class="report-embed-card">
    <div class="report-embed-header">
      <div>
        <h2>Most recent report</h2>
        <p>July 2026 · Sentinel-2 composite · threshold 0.14</p>
      </div>
      <a href="flood_reports/reports/bwma_flood_report_2026-07-01_0.14.html" target="_blank" rel="noopener">Open full report ↗</a>
    </div>
    <iframe
      src="flood_reports/reports/bwma_flood_report_2026-07-01_0.14.html"
      class="report-iframe"
      id="report-frame"
      title="Latest BWMA flood acreage report">
    </iframe>
  </div>
</div>
```
